mixin maskedTextField(title, subtitle, placeholder)
    form.text-field_masked
        p.text-field__title.text-H3
            |#{title}
        p.text-field__subtitle.text-H3
            |#{subtitle}
        input.text-field__input(type="text" placeholder=placeholder value!=attributes.value)